import { InputsearchPipe } from './inputsearch.pipe';

describe('InputsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new InputsearchPipe();
    expect(pipe).toBeTruthy();
  });
});
